# Web 框架
fastapi
uvicorn[standard]
pydantic>=2.0.0
pydantic-settings

# Agent 框架
langgraph
langchain
langchain-openai  # 如果使用 OpenAI 模型，或者使用兼容 OpenAI 格式的模型（如 DeepSeek, 通义千问等）
# langchain-anthropic # 如果使用 Claude 模型则开启此行

# 数据处理与工具
numpy
pandas
python-dotenv
httpx

# 可选：状态持久化（生产环境建议使用 Redis）
# redis